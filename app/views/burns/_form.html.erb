<div class="w-100">
  <%= form_with(model: burn, local: true) do |form| %>
    <% if burn.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(burn.errors.count, "error") %> prohibited this burn from being saved:</h2>
        <ul>
          <% burn.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div class="row">
      <%= form.label :burn, class: "w-100" %>
    </div>
    <div class="row">
        <%= form.text_area :burn, class: "w-100 border border-primary rounded regina_color" %>
    </div>
    <div class="row">
        <%= form.button :submit, class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
